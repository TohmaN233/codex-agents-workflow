interface:
  display_name: "Codex Agents Workflow Orchestration"
  short_description: "Coordinate native agents when no Workflow fits"
  default_prompt: "Use $orchestration to declare a SELECTIVE ROUTE before task tools; select the implementation and review route appropriate to the task."
