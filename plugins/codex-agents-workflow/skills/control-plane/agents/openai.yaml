interface:
  display_name: "Codex Agents Workflow"
  short_description: "Find and run Workflows for collaborative tasks"
  default_prompt: "Use $workflow-control-plane to find a Workflow suited to my task, run its steps, and verify the result."
