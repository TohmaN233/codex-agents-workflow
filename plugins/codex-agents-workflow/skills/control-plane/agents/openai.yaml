interface:
  display_name: "Codex Agents Workflow"
  short_description: "Run versioned Workflows with pinned Providers and explicit main acceptance"
  default_prompt: "Use $workflow-control-plane; read metadata once, run a pinned Workflow, and verify actual evidence."
