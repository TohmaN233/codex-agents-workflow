// One acceptance contract shared by planning and review through analysis/request.txt.
export const CONVERSION_CONTRACT = {
  // v8 adds a complete source-section disposition ledger so omission and
  // reference-only pruning are explicit semantic decisions rather than silent
  // information loss.
  // imported Draft's baseline requirements part of the shared planning/review
  // packet. This prevents either model from rediscovering or contradicting facts
  // that the coarse importer already established.
  version: 8,
  checks: [
    'Analyze independent work versus data/order/shared-state dependencies. Use parallel/join for useful disjoint tasks and justify sequential decisions. When the source requires one worker per item or simultaneous execution, represent that fan-out structurally or with an explicit dynamic-worker contract, typed per-item outputs and a join; vague “use concurrency” prose is not coverage.',
    'Decide Main versus a Codex task thread for each responsibility. Main owns synthesis and user-facing decisions; delegate bounded independently verifiable work to a user-visible task thread. Legacy Provider/subagent execution remains supported for existing definitions and fixed routes; in fixed mode use configured routes and omit automatic execution selection fields.',
    'Identify required human intervention and missing task answers before generating the graph; preserve decision timing and distinguish approval gates from input collection.',
    'In automatic mode choose each Codex task thread from the pinned registered native catalog using suitability descriptions and actual capabilities with a task-specific reason. In fixed mode follow configured task routes, including legacy Provider/subagent routes. Never infer model quality from names.',

    'Preserve all meaningful source phases and their dependency order: discovery, production, evaluation, feedback and delivery. Enumerate materially different request modes such as setup-only versus execution, give each an explicit condition and terminal route, and do not force every mode through the full production chain. Do not approve a plan before the source-required evidence gathering.',
    'Preserve hard rules either explicitly or through actionable instructions to read and apply exact pinned references. Reference inheritance is valid coverage; do not duplicate every implementation detail in every prompt.',
    'Build a source-requirement inventory before graph acceptance. Map every material requirement to concrete nodes, bindings, runtime guards or pinned resources and mark any remaining Agent interpretation or unsupported behavior explicitly. Cite the exact spans that define schemas, helper interfaces, dependencies and per-item rules instead of an umbrella whole-file span. A proposal-side checklist is not proof of source coverage.',
    'Connect each consumed result to an actual preceding producer or explicit future Run input. If evaluation or correction replaces an artifact, every approval and delivery node must bind the corrected artifact rather than the stale pre-evaluation producer. Do not invent input fields, gate responses, output values or completed artifacts.',
    'Success means the result needed downstream was actually produced. When inputs, capabilities or verification are missing, the node must stop, not return a successful blocker report. Preserve source-defined successful terminal modes such as setup readiness instead of misclassifying them as missing-input failure. In Strict execution a node returns exactly {"$workflow_blocked":"specific reason or missing input"} to fail visibly and prevent downstream success edges; this is the sole exception to its success output schema. Cooperative host execution reports failure through workflow_fail_node; do not hardcode Strict-only failure instructions in a Cooperative graph.',
    'Agent nodes cannot pause for a conversation and human gates are not forms. Required briefing answers and startup continuation decisions must be supplied explicitly in the future Run task after source-required evidence gathering. If answers are missing, report the concrete questions via the blocking result; a later Run supplies the answers. Preserve startup memory/continuation and feedback procedures by actionable pinned reference.',
    'Keep human confirmation before the dependent work it authorizes. Approval never enables missing tools or grants extra permissions. Gate source-required user decisions, not static checks of the current host environment.',
    'Keep optional features, installation, credentials and network conditional on the source trigger. Declare unconditional executable names with source evidence in required_executables for the automatic Run preparation gate. Keep optional tools out of that unconditional list and check them before their triggered step. Preserve valid cache-reuse conditions; do not turn conditional dependencies into universal blockers.',
    'Separate the intended workflow from present execution capability. Preserve intended phases and portable resource references in the editable Draft. Declare source dependencies without embedding current host availability; environment preparation and failures belong to execution. Do not replace the workflow with a blocker-report-only graph, and never simulate unavailable execution or claim Ready.',
    'Every inferred node and edge must have an actual supporting source span. An edge citation covers the source-to-target transition, including the downstream phase; a confirmation citation must support that specific confirmation, not a different earlier gate. Broaden spans when multiple source rules justify the item; confidence is not proof. Finish a source-span pass over every edge and gate even when other blocking findings were already found.',
    'Review conversion correctness, not implementation quality of the imported software. Report all material violations in one pass with affected node, source evidence and minimal required correction. Styling preferences are not blockers.',
    'Preserve every explicit artifact or interface contract: output paths, filenames, JSON keys and value types, CSV columns, callable signatures, rounding and other stated format constraints. A generic “produce the result” node is not coverage.',
    'Identify deterministic, semantics-preserving output canonicalization separately from semantic work. When an exact pre-authorized host tool exists, bind canonicalization and revalidation to that tool, journal its changed paths, and do not spend another model turn asking an Agent to reformat the same values. Without an exact trusted implementation, mark the requirement agent_assisted or unsupported; never claim it is fully compiled, invent a host tool, or silently alter semantic content.',
    'Preserve concrete method fidelity: named algorithms, parameters, coordinate systems, thresholds, ordering, and explicit forbidden alternatives. Pin the smallest source reference needed for the responsible node; do not replace a prescribed method with an ungrounded equivalent.',
    'Bind dependencies to their phase and trigger. Unconditional executables are derived only from typed dependency requirements; conditional dependencies stay conditional. A name in required_executables without its requirement mapping is not evidence.',
    'For each material contract, ensure validation is at least as strong as the source requirement where validation is possible. Do not claim a schema, artifact, or method is fully compiled based on a model prompt alone.',
    'Cross-check source resources, the imported Draft baseline requirements, static import observations, requirement inventory, node resource projections, bindings and the compiled graph together. For every supporting resource selected by a node or mapping, reconcile its concrete dependencies and interfaces before submission. Do not ask the proposal to echo host-preserved baseline requirements, and do not let a review pass override deterministic coverage findings.',
    'Classify every host-observed source section exactly once as workflow, conditional, reference, or omit. Required rule/process sections must remain actionable; reference-only and omitted sections need a source-grounded rationale. Worked examples, optional taste and illustrative values must not silently become universal requirements, while hard rules, anti-patterns and workflow order must not silently disappear.',
  ],
  reading: 'Read the numbered SKILL.md in this packet once. Resolve only references needed for concrete claims. Use read_workflow_resource_range on large files; each response declares its range and total lines. Never treat a partial read as a full-file audit. Avoid rereading unchanged text.',
  output: 'Emit the smallest complete graph matching the compiler contract. Preserve source procedures by reference rather than verbose paraphrases. One bounded self-check against every check above before submission; unresolved real issues remain explicit.',
};

// Stable identifiers are the shared generator/reviewer vocabulary.
const keys = ['parallelism','agent_ownership','human_intervention','model_selection','phase_order','hard_rules','requirement_coverage','data_handoffs','failure_semantics','conversation_inputs','human_confirmation','conditional_dependencies','portable_artifact','source_support','review_scope','artifact_interface_contract','host_canonicalization','method_fidelity','dependency_binding','validation_strength','cross_resource_consistency','source_disposition'];
CONVERSION_CONTRACT.checks = CONVERSION_CONTRACT.checks.map((requirement,index)=>({id:keys[index],requirement}));
