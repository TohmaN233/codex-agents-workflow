import {useState} from 'react';
import {api,Details,type Json} from './shared';
export function LocalClients(){const [value,setValue]=useState<Json|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState(false);return <section><h2>本机客户端与模型</h2><p>自动发现本机最新版 Codex；Grok 和 Cursor 读取已注册客户端的能力。高级路径覆盖使用 CODEX_CATALOG_BINARY、GROK_BIN、CURSOR_EXE。</p><button disabled={busy} onClick={async()=>{setBusy(true);setError('');try{setValue(await api('local_clients'));}catch(e:any){setError(e.message);}finally{setBusy(false);}}}>{busy?'正在读取…':'刷新本机客户端与模型'}</button>{error && <p role="alert">{error}</p>}{value && <><p>模型来源：Codex {value.codex.source.version}；{value.codex.models.length} 个模型。</p><ul>{value.codex.models.map((m:Json)=><li key={m.model}>{m.displayName || m.model} · {m.model}</li>)}</ul><Details title="客户端路径、能力与执行器资格" value={value}/><p>模型目录可见性与执行器兼容性分别检查；目录更新不自动替换正在运行的执行器。</p></>}</section>}
